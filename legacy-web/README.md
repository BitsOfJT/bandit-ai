# Shelved web UI + Go CLI
#
# This directory holds the previous Bandit stack (React/Vite web app and Go CLI),
# archived when the project moved to a Python-only CLI. It is kept for reference
# and is not part of the active build.
